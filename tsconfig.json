{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "compilerOptions": {
    "baseUrl": ".",	   // 절대 경로의 기준을 프로젝트 루트로 설정
    "paths": {
      "@/*": ["src/*"] // '@'를 'src' 폴더로 매핑
    }
  }
}
